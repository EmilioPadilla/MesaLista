# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

This app is pinned to **Expo SDK 54** (matching `PRAX/apps/mobile`) so it runs in the App Store Expo Go. Do not bump to a newer SDK (e.g. `expo@latest`) until Expo Go ships support for it.
