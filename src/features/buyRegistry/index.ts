// Public API for the buyRegistry feature.
// Internal components (CartDrawer, GiftDetailsModal) stay private — the page
// is the only external entry point.

export { BuyGiftsPage } from './pages/BuyGiftsPage';
