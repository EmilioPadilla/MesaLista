// Public API for the giftLists feature.
// Internal components (GiftListCard, InvitationButton) are not re-exported —
// they are only composed by the feature's own pages.

export { MyGiftListsPage } from './pages/MyGiftListsPage';
